app:
  name: Invest Bot
  config:
    token: ${INVEST_API_TOKEN:}

server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: logfile

logging:
  file:
    name: /tmp/app.log
