app:
  name: Invest Bot
  config:
    token: ${INVEST_API_TOKEN:}
    sandbox: ${INVEST_API_SANDBOX_ON:false}

server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: logfile

logging:
  file:
    name: /tmp/app.log
